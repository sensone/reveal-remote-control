'use strict';

const React = require('react/addons');

require('styles/Zoom.styl');

class Zoom {
  render() {
    return (
      <div>
        <p>Content for Zoom</p>
      </div>
    );
  }
}

module.exports = Zoom;

